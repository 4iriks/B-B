services:
  web:
    image: beat-and-beam-web:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: beat-beam-web
    environment:
      NEXT_TELEMETRY_DISABLED: "1"
      NODE_ENV: production
    ports:
      - "3000:3000"
    restart: unless-stopped
